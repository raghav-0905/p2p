apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-frontend
  namespace: p2p
  labels:
    app: vendor-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vendor-frontend
  template:
    metadata:
      labels:
        app: vendor-frontend
    spec:
      containers:
        - name: vendor-frontend
          image: <AWS_ACCOUNT_ID>.dkr.ecr.ap-south-1.amazonaws.com/p2p-vendor-frontend:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: vendor-frontend-svc
  namespace: p2p
spec:
  selector:
    app: vendor-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
